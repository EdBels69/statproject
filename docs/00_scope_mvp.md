# Stat Analyzer — MVP Technical Specification

> Целевой план разработки MVP на основе встречного проекта.  
> **4 движка + workspace storage + rule-based decision engine.**  
> LLM только объясняет, не выбирает и не считает статистику.

---

## 0) Цель проекта

Создать **тонкий слой** над проверенными библиотеками (SciPy/Statsmodels/Pandas) для отдела научных и клинических исследований:

- Стандартизировать выбор статистических методов (rule-based decision engine)
- Обеспечить воспроизводимость (manifest/audit trail)
- Выпускать отчёт "как для статьи/внутреннего отчёта" в форматах Markdown/HTML
- Дать удобный UX-мастер для ролей: клиницист, мед-аналитик, специалист по КИ/НИОКР, продакт/руководитель

**Ключевой принцип:** Статистику считает движок на библиотеках, **LLM не "выбирает" и не "считает"** статистику, а только объясняет уже рассчитанные результаты (и может быть полностью выключен).

---

## 1) MVP Scope vs Non-goals

### MVP — Функциональный минимум

#### Данные
- **Импорт:** CSV/XLSX
- **Авто-детект типов:** numeric/categorical/date/text
- **Обработка пропусков:** показать, простые опции обработки
- **Профиль датасета:** распределения, выбросы базово, описание по группам

#### Статистика (клинический базовый набор)
- **Нормальность:** Shapiro + QQ
- **2 независимые группы:** t-test / Welch, Mann–Whitney
- **2 парные:** paired t-test, Wilcoxon
- **>2 групп:** ANOVA / Welch ANOVA, Kruskal–Wallis
- **Post-hoc:** Tukey и/или Dunn (минимум 1–2 фиксированных варианта)
- **Корреляции:** Pearson / Spearman
- **Поправки на множественные сравнения:** Holm + BH(FDR)
- **Эффект-сайзы:** d/g, η²/ω², rank-biserial (минимальный набор)

#### Визуализация (5–7 типов)
- box/violin, histogram, scatter, correlation heatmap, qq-plot, (опционально) bar для категориальных

#### Отчёт
- **Генерация отчёта:** Markdown/HTML по ролям/режимам
- **Экспорт:** report.md, report.html, tables.csv, figures.png (если есть)

#### Воспроизводимость
- **Run manifest:** версии библиотек, параметры анализа, seed, хэш датасета, время запуска

#### Blinding-режим (если нужно)
- Замена имён групп на A/B (или Group 1/2) без раскрытия

### Non-goals / Out of scope (вынести в Backlog)

- ML training/prediction, "AI recommender на моделях"
- Survival analysis, time-series, mixed effects, bayesian статистика
- Power analysis как отдельный модуль уровня G*Power
- Встроенный редактор отчёта/файловый менеджер/сложная история проектов
- Многопользовательская авторизация/роль-права (если не критично для MVP)
- Экспорт в DOCX/PDF как блокер (PDF позже через HTML->PDF)

---

## 2) Персоны и режимы вывода

**Роли (переключатель в UI):**

| Роль | Описание | Фокус отчёта |
|-------|----------|---------------|
| **Клиницист** | Врач/исследователь | Выводы + интерпретация + ограничения |
| **Мед-аналитик** | Статистик/аналитик | Допущения, таблицы, эффект-сайзы, детали расчёта |
| **Специалист по КИ/НИОКР** | QA/QI специалист | Audit trail, SAP-лайт, воспроизводимость, blinding |
| **Продакт/руководитель** | Менеджер/руководитель | Шаблоны "пакетов анализа", статус артефактов, краткие summary |

**Правило:** режим меняет глубину текста и набор разделов, но не меняет метод расчёта.

---

## 3) Архитектура MVP: 4 движка + storage

```
┌─────────────────────────────────────────────────────────────────┐
│                    Frontend (React)                           │
│  UX-мастер: Upload → Profile → Plan → Results → Report       │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                    Backend (FastAPI)                           │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  1. Method Registry                                   │   │
│  │  • Метаданные всех методов                             │   │
│  │  • Реализация (builtin + plugins later)               │   │
│  └─────────────────────────────────────────────────────────┘   │
│                              │                                   │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  2. Decision Engine (rule-based)                    │   │
│  │  • Выбор метода по типам переменных, дизайну,       │   │
│  │    результатам checks                                   │   │
│  └─────────────────────────────────────────────────────────┘   │
│                              │                                   │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  3. Analysis Runner                                 │   │
│  │  • Запускает расчёт                                   │   │
│  │  • Упаковывает результат в единый JSON-формат       │   │
│  └─────────────────────────────────────────────────────────┘   │
│                              │                                   │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  4. Report Engine                                   │   │
│  │  • Генерирует отчёт (Markdown/HTML)                  │   │
│  │  • Шаблоны под роли                                 │   │
│  └─────────────────────────────────────────────────────────┘   │
│                              │                                   │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  Storage (workspace)                                 │   │
│  │  • Хранение на диске                                   │   │
│  │  • projects/<project_id>/{datasets, plans, runs}      │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 4) Целевая структура репозитория

```
stat-analyzer/
├── README.md
├── .env.example
├── .gitignore
├── docker-compose.yml                 # опционально
├── Makefile                        # опционально
│
├── docs/                           # Документация
│   ├── 00_scope_mvp.md
│   ├── 01_personas_user_stories.md
│   ├── 02_data_model.md
│   ├── 03_method_registry_spec.md
│   ├── 04_decision_rules.md
│   ├── 05_report_templates.md
│   └── 06_validation.md
│
├── backend/                         # Python backend
│   ├── pyproject.toml
│   ├── requirements.txt
│   ├── .env.example
│   │
│   ├── app/
│   │   ├── __init__.py
│   │   ├── main.py                 # FastAPI entrypoint
│   │   │
│   │   ├── api/                    # API endpoints
│   │   │   ├── __init__.py
│   │   │   ├── routes.py            # Главный роутер
│   │   │   ├── deps.py
│   │   │   ├── datasets.py
│   │   │   ├── plans.py
│   │   │   ├── runs.py
│   │   │   └── reports.py
│   │   │
│   │   ├── core/                   # Ядро
│   │   │   ├── __init__.py
│   │   │   ├── config.py
│   │   │   ├── logging.py
│   │   │   └── exceptions.py
│   │   │
│   │   ├── schemas/                # Pydantic модели
│   │   │   ├── __init__.py
│   │   │   ├── dataset.py
│   │   │   ├── profile.py
│   │   │   ├── plan.py
│   │   │   ├── run.py
│   │   │   ├── result.py
│   │   │   └── manifest.py
│   │   │
│   │   ├── storage/                # Хранение
│   │   │   ├── __init__.py
│   │   │   ├── workspace.py
│   │   │   └── hash_utils.py
│   │   │
│   │   ├── engines/                # 4 движка
│   │   │   ├── __init__.py
│   │   │   │
│   │   │   ├── registry/           # 1. Method Registry
│   │   │   │   ├── __init__.py
│   │   │   │   ├── registry.py
│   │   │   │   ├── specs/
│   │   │   │   │   └── methods_builtin.yml
│   │   │   │   └── impl/
│   │   │   │       ├── __init__.py
│   │   │   │       ├── t_test.py
│   │   │   │       ├── mann_whitney.py
│   │   │   │       ├── anova.py
│   │   │   │       ├── kruskal.py
│   │   │   │       ├── correlations.py
│   │   │   │       ├── posthoc.py
│   │   │   │       ├── assumptions.py
│   │   │   │       └── _template_method.py
│   │   │   │
│   │   │   ├── decision/          # 2. Decision Engine
│   │   │   │   ├── __init__.py
│   │   │   │   ├── decision_engine.py
│   │   │   │   └── rules/
│   │   │   │       ├── __init__.py
│   │   │   │       ├── base.py
│   │   │   │       └── packs.py
│   │   │   │
│   │   │   ├── analysis/          # 3. Analysis Runner
│   │   │   │   ├── __init__.py
│   │   │   │   ├── runner.py
│   │   │   │   ├── result_packager.py
│   │   │   │   └── figures.py
│   │   │   │
│   │   │   ├── reporting/         # 4. Report Engine
│   │   │   │   ├── __init__.py
│   │   │   │   ├── renderer.py
│   │   │   │   └── templates/
│   │   │   │       ├── clinician.md.j2
│   │   │   │       ├── analyst.md.j2
│   │   │   │       ├── qi.md.j2
│   │   │   │       ├── product.md.j2
│   │   │   │       └── _partials/
│   │   │   │           ├── tables.md.j2
│   │   │   │           ├── assumptions.md.j2
│   │   │   │           └── limitations.md.j2
│   │   │   │
│   │   │   └── llm/              # LLM (опционально)
│   │   │       ├── __init__.py
│   │   │       ├── provider.py
│   │   │       └── prompts/
│   │   │           ├── explain_method.txt
│   │   │           ├── explain_results.txt
│   │   │           └── limitations.txt
│   │   │
│   │   └── plugins/               # Plugins (future)
│   │       ├── __init__.py
│   │       ├── interface.py
│   │       ├── loader.py
│   │       └── examples/
│   │           ├── plugin_example/
│   │           │   ├── plugin.yml
│   │           │   ├── methods.yml
│   │           │   ├── impl/
│   │           │   │   └── my_method.py
│   │           │   └── templates/
│   │           │       └── my_role.md.j2
│   │
│   └── tests/                      # Тесты
│       ├── __init__.py
│       ├── conftest.py
│       ├── unit/
│       ├── integration/
│       └── golden/                    # Golden datasets
│           ├── datasets/
│           ├── expected/
│           └── seed_golden.py
│
├── frontend/                        # React frontend
│   ├── package.json
│   ├── vite.config.js
│   ├── tailwind.config.js
│   ├── .env.example
│   │
│   └── src/
│       ├── main.jsx
│       ├── App.jsx
│       │
│       ├── app/                     # Страницы
│       │   ├── pages/
│       │   │   ├── Upload.jsx
│       │   │   ├── Profile.jsx
│       │   │   ├── Plan.jsx
│       │   │   ├── Results.jsx
│       │   │   └── Report.jsx
│       │   │
│       │   └── components/           # Компоненты
│       │       ├── WizardShell.jsx
│       │       ├── VariablePicker.jsx
│       │       ├── ├── GroupPicker.jsx
│       │       ├── ├── AssumptionBadges.jsx
│       │       ├── ├── ResultTables.jsx
│       │       ├── ├── PlotPanel.jsx
│       │       ├── ├── ExportButtons.jsx
│       │       └── common/
│       │           ├── Button.jsx
│       │           ├── Card.jsx
│       │           ├── Modal.jsx
│       │           └── Alert.jsx
│       │
│       └── lib/                     # Утилиты
│           ├── api.ts
│           ├── types.ts
│           ├── packs.ts
│           └── formatters.ts
│
└── scripts/                         # Скрипты
    ├── setup.sh
    ├── dev.sh
    ├── build.sh
    └── deploy.sh
```

---

## 5) Workspace (хранение артефактов)

```
workspace/
└── projects/
    └── <project_id>/
        ├── project.json
        ├── datasets/
        │   └── <dataset_id>/
        │       ├── raw.xlsx
        │       └── profile.json
        ├── plans/
        │   └── <plan_id>.json
        └── runs/
            └── <run_id>/
                ├── manifest.json
                ├── results.json
                ├── figures/
                └── report/
                    ├── report.md
                    └── report.html
```

---

## 6) Метод-реестр: формат метаданных (YAML)

### Пример записи метода (methods_builtin.yml)

```yaml
- id: t_test_independent
  name_ru: "t-критерий Стьюдента (независимые выборки)"
  name_en: "Student t-test (independent)"
  family: "mean_compare"
  design: ["independent_2_groups"]
  requires:
    y: "numeric"
    group: "categorical"
    min_n_per_group: 5
  assumptions:
    - normality
    - homoscedasticity
  checks:
    normality: "shapiro"
    homoscedasticity: "levene"
  effect_sizes: ["cohens_d", "hedges_g"]
  multiple_comparisons: ["holm", "bh_fdr"]
  outputs:
    tables: ["group_descriptives", "test_result", "effect_sizes"]
    plots: ["box", "hist", "qq"]
  references:
    - "guideline_or_textbook_key"
  limitations_text: "Краткое предупреждение/ограничения для отчёта."

- id: mann_whitney
  name_ru: "Тест Манна-Уитни"
  name_en: "Mann-Whitney U test"
  family: "mean_compare"
  design: ["independent_2_groups"]
  requires:
    y: "numeric"
    group: "categorical"
    min_n_per_group: 5
  assumptions:
    - independence
  checks:
    independence: "visual"
  effect_sizes: ["rank_biserial"]
  multiple_comparisons: ["holm", "bh_fdr"]
  outputs:
    tables: ["group_descriptives", "test_result", "effect_sizes"]
    plots: ["box", "hist"]
  references:
    - "guideline_or_textbook_key"
  limitations_text: "Непараметрический аналог t-теста."

- id: anova
  name_ru: "Дисперсионный анализ (ANOVA)"
  name_en: "One-way ANOVA"
  family: "mean_compare"
  design: ["independent_multiple_groups"]
  requires:
    y: "numeric"
    group: "categorical"
    min_n_per_group: 5
  assumptions:
    - normality
    - homoscedasticity
  checks:
    normality: "shapiro"
    homoscedasticity: "levene"
  effect_sizes: ["eta_squared", "omega_squared"]
  multiple_comparisons: ["holm", "bh_fdr"]
  outputs:
    tables: ["group_descriptives", "test_result", "effect_sizes", "posthoc"]
    plots: ["box", "hist", "qq"]
  references:
    - "guideline_or_textbook_key"
  limitations_text: "Требует нормальности и равенства дисперсий."

- id: kruskal_wallis
  name_ru: "Тест Крускала-Уоллиса"
  name_en: "Kruskal-Wallis test"
  family: "mean_compare"
  design: ["independent_multiple_groups"]
  requires:
    y: "numeric"
    group: "categorical"
    min_n_per_group: 5
  assumptions:
    - independence
  checks:
    independence: "visual"
  effect_sizes: ["eta_squared_h"]
  multiple_comparisons: ["holm", "bh_fdr"]
  outputs:
    tables: ["group_descriptives", "test_result", "effect_sizes", "posthoc"]
    plots: ["box", "hist"]
  references:
    - "guideline_or_textbook_key"
  limitations_text: "Непараметрический аналог ANOVA."

- id: pearson_correlation
  name_ru: "Корреляция Пирсона"
  name_en: "Pearson correlation"
  family: "correlation"
  design: ["correlation"]
  requires:
    x: "numeric"
    y: "numeric"
    min_n: 10
  assumptions:
    - linearity
    - normality
  checks:
    linearity: "visual"
    normality: "shapiro"
  effect_sizes: ["r"]
  multiple_comparisons: ["holm", "bh_fdr"]
  outputs:
    tables: ["correlation_matrix", "p_values"]
    plots: ["scatter", "heatmap"]
  references:
    - "guideline_or_textbook_key"
  limitations_text: "Требует линейной зависимости и нормальности."

- id: spearman_correlation
  name_ru: "Корреляция Спирмена"
  name_en: "Spearman correlation"
  family: "correlation"
  design: ["correlation"]
  requires:
    x: "numeric"
    y: "numeric"
    min_n: 10
  assumptions:
    - monotonicity
  checks:
    monotonicity: "visual"
  effect_sizes: ["rho"]
  multiple_comparisons: ["holm", "bh_fdr"]
  outputs:
    tables: ["correlation_matrix", "p_values"]
    plots: ["scatter", "heatmap"]
  references:
    - "guideline_or_textbook_key"
  limitations_text: "Непараметрический аналог корреляции Пирсона."
```

---

## 7) Единый JSON результата

```json
{
  "inputs": {
    "dataset_id": "...",
    "plan_id": "...",
    "variables": {
      "y": "value",
      "group": "treatment",
      "x": null
    },
    "parameters": {
      "alpha": 0.05,
      "correction": "holm",
      "effect_sizes": true
    }
  },
  "assumption_checks": {
    "normality": {
      "method": "shapiro",
      "result": {
        "statistic": 0.95,
        "p_value": 0.23,
        "passed": true
      }
    },
    "homoscedasticity": {
      "method": "levene",
      "result": {
        "statistic": 1.2,
        "p_value": 0.45,
        "passed": true
      }
    }
  },
  "primary_test": {
    "method": "t_test_independent",
    "statistic": 2.17,
    "df": 98,
    "p_value": 0.03,
    "ci": {
      "lower": 1.2,
      "upper": 11.8
    },
    "interpretation": "Различия статистически значимы (p < 0.05)"
  },
  "effect_sizes": {
    "cohens_d": 0.68,
    "interpretation": "Средний размер эффекта"
  },
  "multiple_comparisons": null,
  "tables": {
    "group_descriptives": [
      {
        "group": "A",
        "n": 50,
        "mean": 45.2,
        "sd": 12.3,
        "median": 44.5,
        "iqr": [38.0, 52.0]
      },
      {
        "group": "B",
        "n": 50,
        "mean": 38.7,
        "sd": 11.8,
        "median": 37.5,
        "iqr": [32.0, 45.0]
      }
    ],
    "test_result": {
      "t": 2.17,
      "df": 98,
      "p": 0.03,
      "ci_lower": 1.2,
      "ci_upper": 11.8
    },
    "effect_sizes": {
      "cohens_d": 0.68,
      "interpretation": "Средний размер эффекта"
    }
  },
  "figures": {
    "box": "figures/box.png",
    "hist": "figures/hist.png",
    "qq": "figures/qq.png"
  },
  "narrative_blocks": {
    "method_explanation": "...",
    "interpretation": "...",
    "limitations": "..."
  },
  "manifest": {
    "versions": {
      "scipy": "1.11.0",
      "statsmodels": "0.14.0",
      "pandas": "2.1.0"
    },
    "parameters": {
      "alpha": 0.05,
      "correction": "holm"
    },
    "seed": 42,
    "dataset_hash": "abc123...",
    "timestamp": "2025-01-03T12:30:45Z"
  }
}
```

---

## 8) Decision Engine: rule-based выбор

### Base rules

```python
# backend/app/engines/decision/rules/base.py

def select_method(data_profile, plan):
    """
    Rule-based выбор метода
    """
    # Если 2 независимые группы
    if plan['design'] == 'independent_2_groups':
        # Проверить нормальность
        if data_profile['normality']['passed']:
            # Проверить гомоскедастичность
            if data_profile['homoscedasticity']['passed']:
                return 't_test_independent'
            else:
                return 'welch_t_test'
        else:
            return 'mann_whitney'
    
    # Если >2 независимые группы
    elif plan['design'] == 'independent_multiple_groups':
        if data_profile['normality']['passed']:
            if data_profile['homoscedasticity']['passed']:
                return 'anova'
            else:
                return 'welch_anova'
        else:
            return 'kruskal_wallis'
    
    # Если корреляция
    elif plan['design'] == 'correlation':
        if data_profile['normality']['passed']:
            return 'pearson_correlation'
        else:
            return 'spearman_correlation'
    
    # По умолчанию
    else:
        raise ValueError("Неизвестный дизайн исследования")
```

### Packs (шаблоны под отдел)

```yaml
# backend/app/engines/decision/rules/packs.yml

packs:
  - id: two_group_compare_basic
    name_ru: "Сравнение двух групп (базовый)"
    name_en: "Two group comparison (basic)"
    description: "Базовый пакет для сравнения двух независимых групп"
    design: "independent_2_groups"
    allowed_methods:
      - t_test_independent
      - welch_t_test
      - mann_whitney
    default_method: "auto"
    checks:
      - normality
      - homoscedasticity
    effect_sizes:
      - cohens_d
      - hedges_g
    multiple_comparisons:
      - holm
      - bh_fdr
    plots:
      - box
      - hist
      - qq
    report_sections:
      - introduction
      - methods
      - results
      - interpretation
      - limitations

  - id: multiple_group_compare_basic
    name_ru: "Сравнение нескольких групп (базовый)"
    name_en: "Multiple group comparison (basic)"
    description: "Базовый пакет для сравнения нескольких независимых групп"
    design: "independent_multiple_groups"
    allowed_methods:
      - anova
      - welch_anova
      - kruskal_wallis
    default_method: "auto"
    checks:
      - normality
      - homoscedasticity
    effect_sizes:
      - eta_squared
      - omega_squared
    multiple_comparisons:
      - holm
      - bh_fdr
    posthoc:
      - tukey
      - dunn
    plots:
      - box
      - hist
      - qq
    report_sections:
      - introduction
      - methods
      - results
      - posthoc
      - interpretation
      - limitations

  - id: correlation_basic
    name_ru: "Корреляционный анализ (базовый)"
    name_en: "Correlation analysis (basic)"
    description: "Базовый пакет для корреляционного анализа"
    design: "correlation"
    allowed_methods:
      - pearson_correlation
      - spearman_correlation
    default_method: "auto"
    checks:
      - normality
      - linearity
    effect_sizes:
      - r
    multiple_comparisons:
      - holm
      - bh_fdr
    plots:
      - scatter
      - heatmap
    report_sections:
      - introduction
      - methods
      - results
      - interpretation
      - limitations
```

---

## 9) Минимальный API контракт

### Обязательные эндпоинты

```python
# backend/app/api/routes.py

from fastapi import APIRouter
from app.schemas.dataset import DatasetUpload, DatasetProfile
from app.schemas.plan import Plan, PlanValidation
from app.schemas.run import RunRequest, RunResult, RunManifest
from app.schemas.report import ReportRequest

router = APIRouter()

# Datasets
@router.post("/datasets", response_model=DatasetUpload)
async def upload_dataset(file: UploadFile):
    """Загрузка файла, возвращает dataset_id"""
    pass

@router.get("/datasets/{dataset_id}/profile", response_model=DatasetProfile)
async def get_dataset_profile(dataset_id: str):
    """Профиль данных"""
    pass

# Methods
@router.get("/methods")
async def get_methods():
    """Список методов (из registry)"""
    pass

# Plans
@router.post("/plans/validate", response_model=PlanValidation)
async def validate_plan(plan: Plan):
    """Проверить план на валидность/совместимость"""
    pass

# Runs
@router.post("/runs", response_model=RunResult)
async def run_analysis(request: RunRequest):
    """Запустить анализ (dataset_id + plan)"""
    pass

@router.get("/runs/{run_id}", response_model=RunResult)
async def get_run(run_id: str):
    """Получить results.json"""
    pass

@router.get("/runs/{run_id}/manifest", response_model=RunManifest)
async def get_run_manifest(run_id: str):
    """Получить manifest"""
    pass

# Reports
@router.get("/runs/{run_id}/report")
async def get_report(
    run_id: str,
    role: str = "clinician",
    format: str = "md"
):
    """Получить отчёт по роли и формату"""
    pass

# Artifacts
@router.get("/runs/{run_id}/artifacts")
async def get_artifacts(run_id: str):
    """Ссылки на таблицы/фигуры"""
    pass
```

---

## 10) UX-мастер (frontend): 4–5 шагов

### Step 1: Upload

```
┌─────────────────────────────────────────────────────────────────┐
│  Загрузка данных                                           │
│                                                           │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                                                         │   │
│  │              📁                                      │   │
│  │                                                         │   │
│  │        Перетащите файл сюда                             │   │
│  │                                                         │   │
│  │        или нажмите для выбора                              │   │
│  │                                                         │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                           │
│  Поддерживаемые форматы: CSV, XLSX                        │
│                                                           │
│  [Далее →]                                                │
│                                                           │
└─────────────────────────────────────────────────────────────────┘
```

### Step 2: Profile

```
┌─────────────────────────────────────────────────────────────────┐
│  Профиль данных                                            │
│                                                           │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  Типы колонок                                        │   │
│  │  ┌──────────┬──────────┬──────────┬──────────┐ │   │
│  │  │ Колонка  │ Тип      │ Пропуски │ Уникальных│ │   │
│  │  ├──────────┼──────────┼──────────┼──────────┤ │   │
│  │  │ value    │ numeric  │ 0        │ 45       │ │   │
│  │  │ group    │ category │ 0        │ 2        │ │   │
│  │  │ id       │ numeric  │ 0        │ 100      │ │   │
│  │  └──────────┴──────────┴──────────┴──────────┘ │   │
│  │                                                         │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                           │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  Базовая статистика                                   │   │
│  │  N = 100, Среднее = 41.95, SD = 12.05           │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                           │
│  [← Назад]  [Далее →]                                    │
│                                                           │
└─────────────────────────────────────────────────────────────────┘
```

### Step 3: Plan

```
┌─────────────────────────────────────────────────────────────────┐
│  План анализа                                             │
│                                                           │
│  Выберите пакет анализа:                                    │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  ○ Сравнение двух групп (базовый)                  │   │
│  │  ○ Сравнение нескольких групп (базовый)            │   │
│  │  ○ Корреляционный анализ (базовый)                │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                           │
│  Выберите переменные:                                      │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  Значение (Y): [value ▼]                         │   │
│  │  Группа:        [group ▼]                          │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                           │
│  Параметры:                                                │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  Alpha:      [0.05]                               │   │
│  │  Поправка:   [Holm ▼]                            │   │
│  │  Effect sizes: [✓]                                  │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                           │
│  [← Назад]  [Запустить анализ →]                           │
│                                                           │
└─────────────────────────────────────────────────────────────────┘
```

### Step 4: Results

```
┌─────────────────────────────────────────────────────────────────┐
│  Результаты анализа                                        │
│                                                           │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  ✅ Нормальность: p = 0.23 (пройдено)             │   │
│  │  ✅ Гомоскедастичность: p = 0.45 (пройдено)      │   │
│  │                                                         │   │
│  │  Метод: t-критерий Стьюдента                      │   │
│  │  t(98) = 2.17, p = 0.03                              │   │
│  │  95% CI: [1.2, 11.8]                                 │   │
│  │                                                         │   │
│  │  Effect size: Cohen's d = 0.68 (средний)            │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                           │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  [Box Plot]  [Histogram]  [QQ Plot]              │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                           │
│  [← Назад]  [Отчёт →]                                   │
│                                                           │
└─────────────────────────────────────────────────────────────────┘
```

### Step 5: Report

```
┌─────────────────────────────────────────────────────────────────┐
│  Отчёт                                                    │
│                                                           │
│  Роль: [Клиницист ▼]                                     │
│                                                           │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  ## Результаты анализа                                │   │
│  │                                                         │   │
│  │  Различия между группами статистически значимы   │   │
│  │  (p = 0.03 < 0.05). Среднее значение группы    │   │
│  │  А (45.2) больше группы Б (38.7). Размер эффекта │   │
│  │  средний (Cohen's d = 0.68).                       │   │
│  │                                                         │   │
│  │  ### Ограничения                                      │   │
│  │  Требует нормальности распределения и равенства   │   │
│  │  дисперсий.                                            │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                           │
│  Экспорт: [Markdown] [HTML] [CSV] [PNG]                   │
│                                                           │
│  [← Назад]  [Новый анализ]                                │
│                                                           │
└─────────────────────────────────────────────────────────────────┘
```

---

## 11) LLM (опционально): правила безопасности

### Правила

1. **LLM может быть выключен** (`AI off`) и всё должно работать без него
2. **В LLM не отправлять "сырые строки"** датасета, только агрегированные результаты/параметры
3. **LLM делает только:**
   - Объяснение выбора теста (по шаблону)
   - Интерпретацию результатов (по шаблону)
   - Блок "ограничения" (по чек-листу)
4. **Любые "AI recommends method" — запрещено**, метод выбирает decision engine

### Пример промпта

```python
# backend/app/engines/llm/prompts/explain_results.txt

Ты — эксперт по статистике, объясняющий для {role}.

Результаты анализа:
- Метод: {method_name}
- p-value: {p_value}
- Effect size: {effect_size}
- 95% CI: {ci_lower}, {ci_upper}

Объясни результаты простым языком, без сложной терминологии.

Включи:
1. Что означает p-value
2. Как интерпретировать effect size
3. Какие есть ограничения метода

Не используй Markdown, только простой текст.
```

---

## 11.5) Интеграция с GLM API

### Провайдер GLM

```python
# backend/app/engines/llm/provider.py

import httpx
import os
from typing import Optional

class GLMProvider:
    """Провайдер для GLM API (https://open.bigmodel.cn/)"""
    
    def __init__(self):
        self.api_key = os.getenv("GLM_API_KEY")
        self.api_url = os.getenv(
            "GLM_API_URL",
            "https://open.bigmodel.cn/api/paas/v4/chat/completions"
        )
        self.model = os.getenv("GLM_MODEL", "glm-4")
        self.enabled = os.getenv("GLM_ENABLED", "true").lower() == "true"
    
    async def generate(self, prompt: str) -> Optional[str]:
        """
        Генерация текста через GLM API
        
        Args:
            prompt: Текст промпта
            
        Returns:
            Сгенерированный текст или None если LLM выключен
        """
        if not self.enabled:
            return None
        
        if not self.api_key:
            return None
        
        try:
            async with httpx.AsyncClient(timeout=30.0) as client:
                response = await client.post(
                    self.api_url,
                    headers={
                        "Authorization": f"Bearer {self.api_key}",
                        "Content-Type": "application/json"
                    },
                    json={
                        "model": self.model,
                        "messages": [
                            {"role": "user", "content": prompt}
                        ],
                        "temperature": 0.7,
                        "max_tokens": 1000
                    }
                )
                response.raise_for_status()
                result = response.json()
                return result["choices"][0]["message"]["content"]
        except Exception as e:
            # Логируем ошибку, но не прерываем работу
            print(f"GLM API error: {e}")
            return None
    
    def is_available(self) -> bool:
        """Проверка доступности LLM"""
        return self.enabled and bool(self.api_key)
```

### Конфигурация (.env)

```env
# GLM API
GLM_ENABLED=true
GLM_API_KEY=your_glm_api_key_here
GLM_API_URL=https://open.bigmodel.cn/api/paas/v4/chat/completions
GLM_MODEL=glm-4
```

### Использование в Report Engine

```python
# backend/app/engines/reporting/renderer.py

from app.engines.llm.provider import GLMProvider

class ReportRenderer:
    def __init__(self):
        self.llm = GLMProvider()
    
    async def render_explanation(self, role: str, results: dict) -> str:
        """
        Генерация объяснения через LLM
        
        Args:
            role: Роль пользователя (clinician/analyst/qi/product)
            results: Результаты анализа
            
        Returns:
            Сгенерированный текст или шаблонный текст
        """
        # Формируем промпт
        prompt = self._build_prompt(role, results)
        
        # Пытаемся сгенерировать через LLM
        llm_text = await self.llm.generate(prompt)
        
        if llm_text:
            return llm_text
        
        # Если LLM недоступен, используем шаблон
        return self._get_template_explanation(role, results)
    
    def _build_prompt(self, role: str, results: dict) -> str:
        """Формирование промпта для LLM"""
        method = results.get('primary_test', {}).get('method', '')
        p_value = results.get('primary_test', {}).get('p_value', None)
        effect_size = results.get('effect_sizes', {})
        
        return f"""Ты — эксперт по статистике, объясняющий для {role}.

Результаты анализа:
- Метод: {method}
- p-value: {p_value}
- Effect size: {effect_size}

Объясни результаты простым языком, без сложной терминологии.

Включи:
1. Что означает p-value
2. Как интерпретировать effect size
3. Какие есть ограничения метода

Не используй Markdown, только простой текст."""
```

---

## 12) Валидация (обязательная для доверия)

### Golden datasets

```
tests/golden/datasets/
├── two_groups_normal.csv
├── two_groups_non_normal.csv
├── multiple_groups.csv
└── correlation.csv
```

### Expected results

```
tests/golden/expected/
├── two_groups_normal.json
├── two_groups_non_normal.json
├── multiple_groups.json
└── correlation.json
```

### Тест

```python
# tests/integration/test_golden.py

import pytest
from app.engines.analysis.runner import AnalysisRunner

@pytest.mark.parametrize("dataset,expected", [
    ("two_groups_normal", "two_groups_normal.json"),
    ("two_groups_non_normal", "two_groups_non_normal.json"),
])
def test_golden_dataset(dataset, expected):
    """Тест на golden datasets"""
    runner = AnalysisRunner()
    result = runner.run(dataset)
    
    # Сравнить с ожидаемыми значениями
    assert result['primary_test']['p_value'] == pytest.approx(expected['p_value'], rel=1e-3)
    assert result['primary_test']['statistic'] == pytest.approx(expected['statistic'], rel=1e-3)
```

---

## 13) Roadmap

### v0 (MVP)

- Core workflow + базовый набор методов + отчёт по ролям + manifests + packs

### v1

- Расширение библиотеки методов (но по той же схеме registry/impl)
- Улучшение отчётов/таблиц/графиков
- SQLite индекс истории (опционально)

### v2+ (plugins)

- Survival/time-series/mixed effects/bayes/power/ML — только как плагины

---

## 14) План разработки (1 месяц)

### Неделя 1: Фундамент

| День | Задачи | Результат |
|------|----------|-----------|
| 1-2 | Настройка проекта (React + Python) | Работающий шаблон |
| 3-4 | Загрузка Excel/CSV | Можно загружать данные |
| 5-7 | Парсинг и валидация данных | Данные готовы для анализа |

### Неделя 2: Статистика

| День | Задачи | Результат |
|------|----------|-----------|
| 8-10 | Method Registry + Decision Engine | Выбор методов работает |
| 11-12 | Analysis Runner + t-test | t-test работает |
| 13-14 | ANOVA + Mann-Whitney | Базовые тесты работают |

### Неделя 3: Отчёты и валидация

| День | Задачи | Результат |
|------|----------|-----------|
| 15-17 | Report Engine + шаблоны | Генерация отчётов |
| 18-19 | Golden datasets + тесты | Валидация работает |
| 20-21 | LLM интеграция (опционально) | Объяснения работают |

### Неделя 4: Полировка

| День | Задачи | Результат |
|------|----------|-----------|
| 22-23 | UX улучшения | Приятный интерфейс |
| 24-25 | Тестирование и багфикс | Стабильная работа |
| 26-28 | Документация | Понятно как использовать |
| 29-30 | Деплой | Работающий MVP |

---

## 15) Разделение ответственности (AI vs Человек)

### AI делает (я)

- Создание всех файлов проекта
- Написание всего кода (frontend + backend)
- Документация
- Архитектура

### Человек делает (вы)

- Установка зависимостей (`npm install`, `pip install`)
- Получение GLM API ключ на https://open.bigmodel.cn/
- Настройка `.env` файла
- Запуск frontend и backend
- Тестирование в браузере
- Деплой

---

## Заключение

Этот план — **реалистичный MVP** на 1 месяц для одного разработчика с AI-помощью.

**Ключевые принципы:**
1. Rule-based decision engine (не LLM)
2. 4 движка + workspace storage
3. Четкий scope с non-goals
4. Валидация через golden datasets
5. Масштабируемость через plugins (future)
6. LLM опционально (GLM API от https://open.bigmodel.cn/)
7. Всё работает без LLM
